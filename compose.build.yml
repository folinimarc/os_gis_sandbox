# Start dev setup (building images locally):
# docker compose -p sandbox_dev -f compose.yml -f compose.build.yml --env-file sandbox.conf up --build

services:
  jupyterlab:
    build: ./jupyterlabgeoenv/
    image: !reset null
   
  content:
    build: ./content/
    image: !reset null

  hub:
    build: ./hub/
    image: !reset null

  geoserver:
    platform: linux/amd64

  pgadmin:
    platform: linux/amd64

  postgis:
    platform: linux/amd64
    