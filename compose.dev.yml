# Start dev setup (building images locally):
# docker compose -p sandbox_dev -f compose.yml -f compose.dev.yml --env-file sandbox.conf up --build

services:
  jupyterlab:
    build: ./jupyterlabgeoenv/
    image: !reset null
   
  content:
    build: ./content/
    image: !reset null

  hub:
    build: ./hub/
    image: !reset null
    